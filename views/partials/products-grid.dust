
<section class="product-grid twelve columns alpha omega">
	{#products}
		<div class="four columns">
			<a href="/catalog/category/{categoryId}/product/{_id}/{name}" id="product-{id}" class="__animated __fadeInUpBig">
				<img src="http://static.zara.net/photos{colors[0].pictures[0]}" alt="Blue ripped jeans" class="product" />
				<h3>{name}</h3>
				<h4>${price} </h4>
			</a>
		</div>

		{@if cond="({$idx} + 1) % 3 === 0"}
			<br class="clear">
		{/if}
	{/products}
</section>
